package character;

public class Enemy extends Character {
    public Enemy(int hp, int mp, int maxHp, int maxMP, int strength, int defence) {
        super(hp, mp, maxHp, maxMP, strength, defence);
    }


}
